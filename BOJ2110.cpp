#include <stdio.h>
#include <algorithm>
#include <utility>
#include <vector>

using namespace std;
typedef pair<int, pair<int, int>> P;
int N, K;
bool set[200001];
vector<P> edge;

int main(){
    scanf("%d %d", &N, &K);
    return 0;
}